<div className="bg-card/90 border border-dim rounded-2xl shadow-elev-1 p-5">
  <h2 className="text-2xl font-semibold mb-1"># Redeem Voucher</h2>
  <p className="text-soft mb-4">Enter voucher code or scan QR to process customer redemption</p>

  {/* Tabs */}
  <div className="mb-4 inline-flex rounded-xl border border-dim bg-surface overflow-hidden">
    <button
      className={`px-4 py-2 text-sm ${activeTab==='manual'
        ? 'bg-surface-2 text-fg'
        : 'text-soft hover:bg-white/[0.03]'
      }`}
      onClick={() => setActiveTab('manual')}
    >
      Manual Entry
    </button>
    <button
      className={`px-4 py-2 text-sm ${activeTab==='qr'
        ? 'bg-surface-2 text-fg'
        : 'text-soft hover:bg-white/[0.03]'
      }`}
      onClick={() => setActiveTab('qr')}
    >
      QR Scan
    </button>
  </div>

  {activeTab === 'manual' ? (
    <form onSubmit={handleSubmit(onSubmit)} className="space-y-4">
      <div>
        <label className="block text-sm font-medium tracking-wide text-fg mb-1">Voucher Code</label>
        <input className="input-dark" placeholder="Enter voucher code" {...register('code')} />
      </div>

      <div>
        <label className="block text-sm font-medium tracking-wide text-fg mb-1">Staff PIN</label>
        <input className="input-dark" placeholder="Enter your staff PIN" {...register('pin')} />
      </div>

      <div>
        <label className="block text-sm font-medium tracking-wide text-fg mb-1">
          Basket Amount <span className="text-soft">(Optional)</span>
        </label>
        <input className="input-dark" placeholder="£0.00" {...register('basket')} />
        <p className="text-xs text-soft/80 mt-1">Enter basket total for percentage discounts</p>
      </div>

      <div className="pt-2">
        <button type="submit" className="rounded-xl bg-gradient-to-r from-brand1 to-brand2 text-white px-4 py-2 shadow-elev-1">
          Redeem
        </button>
      </div>
    </form>
  ) : (
    <div className="rounded-xl bg-surface border border-dim p-4">
      {/* QR scanner panel */}
      <p className="text-soft">Camera preview and scanner go here.</p>
    </div>
  )}
</div>